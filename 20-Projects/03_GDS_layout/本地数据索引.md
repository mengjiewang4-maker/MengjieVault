# GDS 本地数据索引

更新日期：2026-05-28

这个目录保留 GDS 生成脚本、参数说明、运行记录和轻量 Markdown。`.gds` 版图文件体积大，不适合直接放进 GitHub，统一放到本地数据区。

## 当前本地数据位置

主目录：

`90-Local_Not_Upload/Data/03_GDS_layout/`

本次整理移动内容：

| 来源目录 | 本地数据目录 | 内容 |
|---|---|---|
| `20-Projects/03_GDS_layout/fail/` | `90-Local_Not_Upload/Data/03_GDS_layout/fail/` | 失败版图、历史版图、备份版图中的 `.gds` 文件 |

## 本次整理统计

- 共移动：151 个 `.gds` 文件
- 总大小：约 3.3GB
- 项目目录中的 `fail/` 现在只保留脚本、README、参数表、运行记录等轻量文件

## 使用建议

1. Git 仓库中保存 `.py`、`.md`、`.json` 参数说明和运行记录。
2. `.gds`、`.oas` 等版图文件放在 `90-Local_Not_Upload/Data/03_GDS_layout/`。
3. Markdown 记录里写清楚本地 GDS 路径、生成脚本、日期和版本。

## 推荐记录格式

```markdown
## GDS 文件位置

- 本地路径：90-Local_Not_Upload/Data/03_GDS_layout/fail/
- GDS 文件：
- 生成脚本：
- 参数记录：
- 备注：
```
