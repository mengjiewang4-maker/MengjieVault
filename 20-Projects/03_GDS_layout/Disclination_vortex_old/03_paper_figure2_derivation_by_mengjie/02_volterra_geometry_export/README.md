# Volterra Geometry Export

本目录保存 Volterra 切割/粘合几何相关脚本。

`Volterra` 在这里指通过切掉或插入一个扇区来构造 disclination 缺陷的几何方法。

注意：`square_lattice_structure_export_pipeline.py` 引用了当前目录未包含的 `visualize`、`export_comsol`、`export_lumerical`，直接运行前需要补齐依赖。
